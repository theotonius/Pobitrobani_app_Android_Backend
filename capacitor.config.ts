import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sacredword.app',
  appName: 'পবিত্র বানী',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
